# design_prototypes/ — V0.4 设计冲刺静态原型

> **PROTOTYPE ONLY — NOT PRODUCTION**
> 这些 HTML 是设计视觉稿，使用 STATIC DEMO VALUES（如 +$59.79 / SELL DA / PASS），
> 仅用于评审设计方向，**不绑定任何生产逻辑 / DecisionSnapshot**。
> 正式实现（Phase 2）只有在设计方向获批准后才进行，且必须绑定真实数据。

## 文件

| 文件 | 状态 | 说明 |
|---|---|---|
| `01_initial.html` | INITIAL | 初始选择页（选交易 → 生成建议） |
| `02_decision.html` | DECISION_READY | Hero 建议 + 为什么 + 数据依据 + 证据 + 案例 + Lock |
| `03_agent.html` | DECISION_READY | Agent 追问 / Streaming / 工作轨迹 |
| `04_case_e.html` | DECISION_READY | 未来信息被系统拒绝（Time Gate 高潮） |
| `05_reveal.html` | REVEALED | Lock → Reveal → 复盘 |

## 截图

`design_shots/*.png`（1440×900），由 `tools/screenshot_prototype.py` 生成。
